{
  "name": "fenge-energia-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tailwindcss -c tailwind.config.js -i ./src/styles/tailwind.css -o ./css/tailwind.min.css --minify",
    "watch": "tailwindcss -c tailwind.config.js -i ./src/styles/tailwind.css -o ./css/tailwind.min.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.10"
  }
}

